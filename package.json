{
  "name": "android-sms-gateway",
  "private": false,
  "type": "module",
  "scripts": { "start": "bun src/server.ts", "check": "bun build --no-bundle --target=bun src/server.ts --outfile=/dev/null" }
}
